CREATE TABLE funcionarios (
id_funcionario int primary key,
nome varchar (100),
cargo varchar (100),
salario decimal (10,2),
data_admissao date,
data_demissao date,
departamento varchar (100)
);
